# Tasks: Remove Dead Code from DBClient

## 1. Clean up buy/DBClient.py

- [x] Remove all commented-out code and the `__main__` block from `buy/DBClient.py` (5 comment lines + lines 153–181 `if __name__` block), then verify with `ruff check buy/DBClient.py` and `pytest`
